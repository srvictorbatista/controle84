onsorshipsPromotionChannelRenderer");var DCc=new Ck("simpleSurveyMessageRenderer");var ECc={SPAN_ID_UNKNOWN:0,SPAN_ID_ANDROID_MAIN_COLD_HOME_START:1,SPAN_ID_ANDROID_MAIN_HOME_PAGE_SCROLL:2};var FCc={SIGN_IN_METHOD_TYPE_UNKNOWN:0,SIGN_IN_METHOD_TYPE_URL:1,SIGN_IN_METHOD_TYPE_SEAMLESS:2,SIGN_IN_METHOD_TYPE_DIRECT:3,SIGN_IN_METHOD_TYPE_NATIVE:4,SIGN_IN_METHOD_TYPE_MDX_ASSISTED:5,SIGN_IN_METHOD_TYPE_CAST_DIALOG:6,SIGN_IN_METHOD_TYPE_MULTIPLE:7};var GCc={EARLY_BROWSE_REQUEST_ABANDONMENT_TYPE_UNKNOWN:0,EARLY_BROWSE_REQUEST_ABANDONMENT_TYPE_DISABLED:1,EARLY_BROWSE_REQUEST_ABANDONMENT_TYPE_ROUTE:2,EARLY_BROWSE_REQUEST_ABANDONMENT_TYPE_SIGNED_IN:3,EARLY_BROWSE_REQUEST_ABANDONMENT_TYPE_LANGUAGE:4,EARLY_BROWSE_REQUEST_ABANDONMENT_TYPE_MISSING_PAYLOAD:5,EARLY_BROWSE_REQUEST_ABANDONMENT_TYPE_PARAMS:6,EARLY_BROWSE_REQUEST_ABANDONMENT_TYPE_NETWORK_FAILURE:7,EARLY_BROWSE_REQUEST_ABANDONMENT_TYPE_TIMEOUT:8};var HCc={TV_APP_QUALITY_UNKNOWN:0,TV_APP_QUALITY_LIMITED_MEMORY:1,TV_APP_QUALITY_LIMITED_ANIMATION:2,TV_APP_QUALITY_FULL_ANIMATION:3};var ICc={TV_AUTH_ACCESS_CREDENTIAL_TYPE_UNKNOWN:0,TV_AUTH_ACCESS_CREDENTIAL_TYPE_CACHED:1,TV_AUTH_ACCESS_CREDENTIAL_TYPE_IN_FLIGHT:2,TV_AUTH_ACCESS_CREDENTIAL_TYPE_REQUESTED:3,TV_AUTH_ACCESS_CREDENTIAL_TYPE_LOCAL_STORAGE:4};var JCc={TV_NAVIGATION_TYPE_UNKNOWN:0,TV_NAVIGATION_TYPE_FORWARD:1,TV_NAVIGATION_TYPE_BACK:2,TV_NAVIGATION_TYPE_RELOAD:3};var KCc={APP_DIALOG_TYPE_UNKNOWN:0,APP_DIALOG_TYPE_CHROMECAST_UPSELL:1,APP_DIALOG_TYPE_IS_USING_VPN:2,APP_DIALOG_TYPE_IS_TRAVEL:3,APP_DIALOG_TYPE_FAMILY_PLAN:4,APP_DIALOG_TYPE_LOCATION_RESOLUTION:5,APP_DIALOG_TYPE_APP_RATING:6,APP_DIALOG_TYPE_AIRPLAY:7,APP_DIALOG_TYPE_CHECK_IN_WARNING:8,APP_DIALOG_TYPE_CHECK_IN_DELINQUENT:9,APP_DIALOG_TYPE_CHECK_IN_SUCCESSFUL:10,APP_DIALOG_TYPE_GREYLIST_DEVICE:11,APP_DIALOG_TYPE_BLACKLIST_DEVICE:12,APP_DIALOG_TYPE_NFL:13,APP_DIALOG_TYPE_BELL:14,APP_DIALOG_TYPE_WELCOME:15,
APP_DIALOG_TYPE_PUSH_PERMISSION:16,APP_DIALOG_TYPE_NOTIFICATIONS_EDUCATION_SHEET:17,APP_DIALOG_TYPE_VIDEO_PICKER:18,APP_DIALOG_TYPE_INVOL_PAUSED_FAMILY_MANAGER:19,APP_DIALOG_TYPE_INVOL_PAUSED_FAMILY_MEMBER:20,APP_DIALOG_TYPE_TURNER:21,APP_DIALOG_TYPE_VOLUNTARY_PAUSED_FAMILY_MANAGER:22,APP_DIALOG_TYPE_VOLUNTARY_PAUSED_FAMILY_MEMBER:23,APP_DIALOG_TYPE_VOICE_REMOTE:24,APP_DIALOG_TYPE_VOICE_REMOTE_OFFBOARDING:25,APP_DIALOG_TYPE_PACKAGE_UPGRADE:26,APP_DIALOG_TYPE_PAYWALL_UNLOCK:27,APP_DIALOG_TYPE_PAYWALL_WELCOME:29,
APP_DIALOG_TYPE_MLB_CHECK_IN:28,APP_DIALOG_TYPE_INCEPTION_COUNTRY_BLOCK:30,APP_DIALOG_TYPE_FEATURE_TIER_UPSELL:31,APP_DIALOG_TYPE_MOBILE_NOTIFICATIONS_INSTRUCTION:32,APP_DIALOG_TYPE_END_OF_FREE_PREVIEW:33,APP_DIALOG_TYPE_SIGN_UP_WITH_EXTERNAL_LINK:34,APP_DIALOG_TYPE_2FA_FORCED_LOCATION_UPDATE:35,APP_DIALOG_TYPE_2FA_MISMATCH_IP:36,APP_DIALOG_TYPE_REQUEST_HOME_LOCATION:37};var LCc={UNPLUGGED_INITIAL_SCREEN_ORIGIN_UNKNOWN:0,UNPLUGGED_INITIAL_SCREEN_ORIGIN_SIGN_UP:1,UNPLUGGED_INITIAL_SCREEN_ORIGIN_SIGN_IN:2,UNPLUGGED_INITIAL_SCREEN_ORIGIN_AUTHORIZED:3,UNPLUGGED_INITIAL_SCREEN_ORIGIN_ACCOUNT_SWITCH:4};var MCc={UNPLUGGED_USER_STATE_CHANGE_TYPE_UNKNOWN:0,UNPLUGGED_USER_STATE_CHANGE_TYPE_SIGN_UP:1};var NCc={VIDEO_STREAM_TYPE_UNSPECIFIED:0,VIDEO_STREAM_TYPE_LIVE:1,VIDEO_STREAM_TYPE_DVR:2,VIDEO_STREAM_TYPE_VOD:3};var OCc={ACCELERATION_CLIENT_UNKNOWN:0,ACCELERATION_CLIENT_SHORTS:1,ACCELERATION_CLIENT_STORIES:2};var PCc={ENGAGEMENT_PANEL_VISIBILITY_UNSPECIFIED:0,ENGAGEMENT_PANEL_VISIBILITY_EXPANDED:1,ENGAGEMENT_PANEL_VISIBILITY_COLLAPSED:2,ENGAGEMENT_PANEL_VISIBILITY_HIDDEN:3,ENGAGEMENT_PANEL_VISIBILITY_FULL_BLEED:4};var QCc={WEB_VIEW_USE_CASE_UNSPECIFIED:0,WEB_VIEW_USE_CASE_SHOPPING_CART_CHECKOUT:1,WEB_VIEW_USE_CASE_SHOPPING_DIRECT_CHECKOUT:2,WEB_VIEW_USE_CASE_SHOPPING_REVIEWS:3,WEB_VIEW_USE_CASE_SHOPPING_ORDER_HISTORY:4,WEB_VIEW_USE_CASE_SEARCH_WEB_RESULT:5,WEB_VIEW_USE_CASE_SEARCH_WEATHER_RESULT:6,WEB_VIEW_USE_CASE_SEARCH_AIR_QUALITY_RESULT:7,WEB_VIEW_USE_CASE_SEARCH_MOVIE_SHOWTIMES_RESULT:8,WEB_VIEW_USE_CASE_SEARCH_PLACE_RESULT:9,WEB_VIEW_USE_CASE_SEARCH_HOTEL_RESULT:10,WEB_VIEW_USE_CASE_GAMEPLAY_MINI_APP:11};var RCc=new Ck("invokeInstrumentManagerAction");var SCc=new Ck("logYpcFlowDismissCommand");var TCc=new Ck("logYpcFlowStartCommand");var UCc=new Ck("ypcCancelRecurrenceCommand");var VCc=new Ck("cancellationFlowState");var WCc=new Ck("unlimitedCreateFamilyEndpoint");var XCc=new Ck("surveyAnswerCardRenderer");var YCc=new Ck("iconBadgeRenderer");var ZCc=new Ck("sponsorshipDialogHeaderRenderer");var X3={UNKNOWN_TYPE:0,USER:74,ARTIST_BASS_COMPACT_KEY:459,TRACK:344,ARTIST_CONTENT_TYPE:493,ARTIST_TOTAL_REACH_TYPE:419,IS_TOTAL_REACH_EXTENSION:494,C3_CHANNEL:337,MINUTE:237,HOUR:236,DAY:3,WEEK:78,WEEK_MONDAY:248,MONTH:79,QUARTER:246,YEAR:247,NTH_MINUTE:382,NTH_HOUR:383,NTH_DAY:384,TIMEZONE_ID:235,VIDEO:4,WORLD:186,CONTINENT:126,SUB_CONTINENT:127,COUNTRY:117,PROVINCE:125,SALES_REGION:151,DMA_ID:515,CONTENT_OWNER:6,GROUP:132,GROUP_VIDEO:154,GROUP_PLAYLIST:222,GROUP_USER:155,GROUP_ASSET:221,GROUP_CONTENT_OWNER:156,
SUBSCRIPTION_SOURCE_TYPE:209,SUBSCRIPTION_SOURCE_DETAIL:210,VIEWS:7,RAW_VIEWS:124,SPAM_VIEWS:96,NON_VIEW_PLAYBACKS:97,WATCH_TIME:93,WATCH_TIME_HOURS:275,AVERAGE_WATCH_TIME:110,AVERAGE_WATCH_PERCENTAGE:120,POTENTIAL_WATCH_TIME:121,IMPRESSIONS:72,VIDEO_THUMBNAIL_IMPRESSIONS:351,SHORTS_FEED_IMPRESSIONS:495,VIDEO_THUMBNAIL_IMPRESSED_VIEWS:377,VIDEO_THUMBNAIL_IMPRESSIONS_VTR:378,FIRST_SHORTS_FEED_IMPRESSIONS:496,FIRST_SHORTS_FEED_VIEWS:497,SHORTS_FEED_IMPRESSIONS_VTR:498,AVERAGE_WATCH_TIME_FROM_VIDEO_THUMBNAIL_IMPRESSIONS:381,
VIDEO_THUMBNAIL_IMPRESSED_WATCH_TIME:386,WATCH_TIME_PER_VIDEO_THUMBNAIL_IMPRESSION:387,APPROXIMATE_WATCH_TIME_PER_VIDEO_THUMBNAIL_IMPRESSION:410,INTERNAL_VIDEO_PERFORMANCE_SND:437,ESTIMATED_UNIQUE_VIEWERS:338,AVERAGE_VIEWS_PER_VIEWER:380,ACTIVE_SUBSCRIBERS:352,PLAYBACK_LOCATION_DETAIL:113,PLAYBACK_LOCATION_TYPE:114,TRAFFIC_SOURCE_DETAIL:82,TRAFFIC_SOURCE_TYPE:112,DISCOVERY_SOURCE_TYPE:354,TOTAL_ESTIMATED_EARNINGS:21,EARNINGS_SOURCE_ALL:523,EARNINGS_SOURCE_TYPE:375,EARNINGS_SOURCE_GROUPED:514,EARNINGS_SOURCE_GROUP_WATCH_PAGE_ADS:525,
EARNINGS_SOURCE_GROUP_MEMBERSHIPS:526,EARNINGS_SOURCE_GROUP_SUPERS:527,EARNINGS_SOURCE_GROUP_SHORTS_FEED_ADS:528,AD_GROSS_REVENUE:302,CPM:136,IMPRESSIONS_CPM:85,AFV_EARNINGS:22,YOUTUBE_EARNINGS:23,TRANSACTION_EARNINGS_ALL:252,TRANSACTION_COUNT:253,AVERAGE_TRANSACTION_AMOUNT:254,AD_EARNINGS:165,SUBSCRIPTION_EARNINGS:304,EARNINGS_PER_THOUSAND_VIEWS:408,IS_FOR_EPM:421,TOTAL_EARNINGS_EPM:422,VIEWS_EPM:423,REVENUE_SCOPE:463,PLAYLIST:63,VIEWER_GENDER:27,VIEWER_AGE:28,VIEWER_PROMILLE:29,SUBSCRIBERS_GAINED:30,
SUBSCRIBERS_LOST:31,SUBSCRIBERS_GAINED_PLUS_LOST:324,SUBSCRIBERS_NET_CHANGE:32,BELL_SUBSCRIBERS_NET_CHANGE:398,SUBSCRIBER_BELL_STATE:399,SUBSCRIBER_REACHABILITY_STATE:400,NOTIFICATION_PUSH_SENDS:411,NOTIFICATION_PUSH_DISCARDED:413,NOTIFICATION_PUSH_STATUS:414,NOTIFICATION_TYPE:415,RATINGS_LIKES:39,RATINGS_LIKES_ADDED:143,RATINGS_LIKES_REMOVED:144,RATINGS_DISLIKES:40,RATINGS_DISLIKES_ADDED:145,RATINGS_DISLIKES_REMOVED:146,VIDEO_REMINDER_SET_NET:504,RATINGS_LIKES_PLUS_DISLIKES:325,PLAYLIST_SAVES_ADDED:262,
PLAYLIST_SAVES_REMOVED:263,PLAYLIST_SAVES_NET_CHANGE:264,PLAYLIST_TYPE:355,PLAYLIST_VIDEOS_ADDED:249,PLAYLIST_VIDEOS_REMOVED:250,PLAYLIST_VIDEOS_NET_CHANGE:251,COMMENTS:46,SHARINGS:49,SHARING_SERVICE:50,ADTYPES:62,MEDIA_TYPE:105,IS_LIVESTREAM:242,STREAM_TYPE:397,PRIMARY_ADTYPES:83,PLAYBACKS:135,ANNOTATION_ID:87,ANNOTATION_TYPE:153,ANNOTATION_IMPRESSIONS:187,ANNOTATION_CLICKS:89,ANNOTATION_CLICKABLE_IMPRESSIONS:88,ANNOTATION_CLICK_RATE:139,ANNOTATION_CLOSES:91,ANNOTATION_CLOSABLE_IMPRESSIONS:104,ANNOTATION_CLOSE_RATE:137,
CLAIMED_STATUS:99,UPLOADER_TYPE:100,NONSUBSCRIBER_VIEWS:326,DEVICE_PLATFORM_TYPE:148,DEVICE_OS_TYPE:149,SUBSCRIBED_TO_UPLOADER:150,SUBSCRIBED_TO_UPLOADER_STATE:395,AUDIENCE_INDEX:166,AUDIENCE_TYPE:199,AUDIENCE_SAMPLES:167,AUDIENCE_RELATIVE:168,AUDIENCE_WATCH_PERCENTILES:169,AUDIENCE_WATCH_RELATIVE:170,AUDIENCE_REWATCH_PERCENTILES:171,AUDIENCE_REWATCH_RELATIVE:172,AUDIENCE_UNIQUE_PERCENTILES:179,AUDIENCE_UNIQUE_RELATIVE:180,AUDIENCE_WATCH_START:512,AUDIENCE_WATCH_END:513,VIDEO_COUNT_FIRST_PUBLISHED:200,
UPLOAD_COUNT_FIRST_PUBLISHED:476,VIDEO_COUNT_NEW:213,ASSET:191,PLAYLIST_STARTS:192,PLAYLIST_EXITS:305,PLAYLIST_EXIT_        *
         *  License: www.highcharts.com/license
         *
         *  !!!!!!! SOURCE GETS TRANSPILED BY TYPESCRIPT. EDIT TS FILE ONLY. !!!!!!!
         *
         * */
        var __extends = (this && this.__extends) || (function () {
                var extendStatics = function (d,
            b) {
                    extendStatics = Object.setPrototypeOf ||
                        ({ __proto__: [] } instanceof Array && function (d,
            b) { d.__proto__ = b; }) ||
                        function (d,
            b) { for (var p in b) if (b.hasOwnProperty(p)) d[p] = b[p]; };
                return extendStatics(d, b);
            };
            return function (d, b) {
                extendStatics(d, b);
                function __() { this.constructor = d; }
                d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
            };
        })();
        var ColumnSeries = SeriesRegistry.seriesTypes.column;
        var extend = U.extend,
            merge = U.merge,
            pick = U.pick;
        /* *
         *
         *  Class
         *
         * */
        /**
         * @private
         * @class
         * @name Highcharts.seriesTypes.variwide
         *
         * @augments Highcharts.Series
         */
        var VariwideSeries = /** @class */ (function (_super) {
                __extends(VariwideSeries, _super);
            function VariwideSeries() {
                /* *
                 *
                 *  Static Properties
                 *
                 * */
                var _this = _super !== null && _super.apply(this,
                    arguments) || this;
                /* *
                 *
                 *  Properties
                 *
                 * */
                _this.data = void 0;
                _this.options = void 0;
                _this.points = void 0;
                _this.relZ = void 0;
                _this.totalZ = void 0;
                _this.zData = void 0;
                return _this;
            }
            /* *
             *
             * Functions
             *
             * */
            VariwideSeries.prototype.processData = function (force) {
                this.totalZ = 0;
                this.relZ = [];
                SeriesRegistry.seriesTypes.column.prototype.processData.call(this, force);
                (this.xAxis.reversed ?
                    this.zData.slice().reverse() :
                    this.zData).forEach(function (z, i) {
                    this.relZ[i] = this.totalZ;
                    this.totalZ += z;
                }, this);
                if (this.xAxis.categories) {
                    this.xAxis.variwide = true;
                    this.xAxis.zData = this.zData; // Used for label rank
                }
                return;
            };
            /**
             * Translate an x value inside a given category index into the distorted
             * axis translation.
             *
             * @private
             * @function Highcharts.Series#postTranslate
             *
             * @param {number} index
             *        The category index
             *
             * @param {number} x
             *        The X pixel position in undistorted axis pixels
             *
             * @param {Highcharts.Point} point
             *        For crosshairWidth for every point
             *
             * @return {number}
             *         Distorted X position
             */
            VariwideSeries.prototype.postTranslate = function (index, x, point) {
                var axis = this.xAxis,
                    relZ = this.relZ,
                    i = axis.reversed ? relZ.length - index : index,
                    goRight = axis.reversed ? -1 : 1,
                    minPx = axis.toPixels(axis.reversed ?
                        (axis.dataMax || 0) + axis.pointRange :
                        (axis.dataMin || 0)),
                    maxPx = axis.toPixels(axis.reversed ?
                        (axis.dataMin || 0) :
                        (axis.dataMax || 0) + axis.pointRange),
                    len = Math.abs(maxPx - minPx),
                    totalZ = this.totalZ,
                    left = this.chart.inverted ?
                        maxPx - (this.chart.plotTop - goRight * axis.minPixelPadding) :
                        minPx - this.chart.plotLeft - goRight * axis.minPixelPadding,
                    linearSlotLeft = i / relZ.length * len,
                    linearSlotRight = (i + goRight) / relZ.length * len,
                    slotLeft = (pick(relZ[i],
                    totalZ) / totalZ) * len,
                    slotRight = (pick(relZ[i + goRight],
                    totalZ) / totalZ) * len,
                    xInsideLinearSlot = (x - (left + linearSlotLeft));
                // Set crosshairWidth for every point (#8173)
                if (point) {
                    point.crosshairWidth = slotRight - slotLeft;
                }
                return left + slotLeft +
                    xInsideLinearSlot * (slotRight - slotLeft) /
                        (linearSlotRight - linearSlotLeft);
            };
            /* eslint-enable valid-jsdoc */
            // Extend translation by distoring X position based on Z.
            VariwideSeries.prototype.translate = function () {
                // Temporarily disable crisping when computing original shapeArgs
                var crispOption = this.options.crisp,
                    xAxis = this.xAxis;
                this.options.crisp = false;
                SeriesRegistry.seriesTypes.column.prototype.translate.call(this);
                // Reset option
                this.options.crisp = crispOption;
                var inverted = this.chart.inverted,
                    crisp = this.borderWidth % 2 / 2;
                // Distort the points to reflect z dimension
                this.points.forEach(function (point, i) {
                    var left,
                        right;
                    if (xAxis.variwide) {
                        left = this.postTranslate(i, point.shapeArgs.x, point);
                        right = this.postTranslate(i, point.shapeArgs.x +
                            point.shapeArgs.width);
                        // For linear or datetime axes, the variwide column should
                        // start with X and extend Z units, without modifying the
                        // axis.
                    }
                    else {
                        left = point.plotX;
                        right = xAxis.translate(point.x + point.z, 0, 0, 0, 1);
                    }
                    if (this.options.crisp) {
                        left = Math.round(left) - crisp;
                        right = Math.round(right) - crisp;
                    }
                    point.shapeArgs.x = left;
                    point.shapeArgs.width = Math.max(right - left, 1);
                    // Crosshair position (#8083)
                    point.plotX = (left + right) / 2;
                    // Adjust the tooltip position
                    if (!inverted) {
                        point.tooltipPos[0] =
                            point.shapeArgs.x +
                                point.shapeArgs.width / 2;
                    }
                    else {
                        point.tooltipPos[1] =
                            xAxis.len - point.shapeArgs.x -
                                point.shapeArgs.width / 2;
                    }
                }, this);
                if (this.options.stacking) {
                    this.correctStackLabels();
                }
            };
            /**
             * Function that corrects stack labels positions
             * @private
             */
            VariwideSeries.prototype.correctStackLabels = function () {
                var series = this,
                    options = series.options,
                    yAxis = series.yAxis;
                var pointStack,
                    pointWidth,
                    stack,
                    xValue;
                for (var _i = 0, _a = series.points; _i < _a.length; _i++) {
                    var point = _a[_i];
                    xValue = point.x;
                    pointWidth = point.shapeArgs.width;
                    stack = yAxis.stacking.stacks[(series.negStacks &&
                        point.y < (options.startFromThreshold ?
                            0 :
                            options.threshold) ?
                        '-' :
                        '') + series.stackKey];
                    if (stack) {
                        pointStack = stack[xValue];
                        if (pointStack && !point.isNull) {
                            pointStack.setOffset(series.chart.plotLeft - ((pointWidth / 2) || 0), pointWidth || 0, void 0, void 0, point.plotX, series.xAxis);
                        }
                    }
                }
            };
            VariwideSeries.compose = VariwideComposition.compose;
            /**
             * A variwide chart (related to marimekko chart) is a column chart with a
             * variable width expressing a third dimension.
             *
             * @sample {highcharts} highcharts/demo/variwide/
             *         Variwide chart
             * @sample {highcharts} highcharts/series-variwide/inverted/
             *         Inverted variwide chart
             * @sample {highcharts} highcharts/series-variwide/datetime/
             *         Variwide columns on a datetime axis
             *
             * @extends      plotOptions.column
             * @since        6.0.0
             * @product      highcharts
             * @excluding    boostThreshold, crisp, depth, edgeColor, edgeWidth,
             *               groupZPadding, boostBlending
             * @requires     modules/variwide
             * @optionparent plotOptions.variwide
             */
            VariwideSeries.defaultOptions = merge(ColumnSeries.defaultOptions, {
                /**
                 * In a variwide chart, the point padding is 0 in order to express the
                 * horizontal stacking of items.
                 */
                pointPadding: 0,
                /**
                 * In a variwide chart, the group padding is 0 in order to express the
                 * horizontal stacking of items.
                 */
                groupPadding: 0
            });
            return VariwideSeries;
        }(ColumnSeries));
        extend(VariwideSeries.prototype, {
            irregularWidths: true,
            pointArrayMap: ['y', 'z'],
            parallelArrays: ['x', 'y', 'z'],
            pointClass: VariwidePoint
        });
        SeriesRegistry.registerSeriesType('variwide', VariwideSeries);
        /* *
         *
         *  Default Export
         *
         * */
        /* *
         *
         *  API Options
         *
         * */
        /**
         * A `variwide` series. If the [type](#series.variwide.type) option is not
         * specified, it is inherited from [chart.type](#chart.type).
         *
         * @extends   series,plotOptions.variwide
         * @excluding boostThreshold, boostBlending
         * @product   highcharts
         * @requires  modules/variwide
         * @apioption series.variwide
         */
        /**
         * An array of data points for the series. For the `variwide` series type,
         * points can be given in the following ways:
         *
         * 1. An array of arrays with 3 or 2 values. In this case, the values correspond
         *    to `x,y,z`. If the first value is a string, it is applied as the name of
         *    the point, and the `x` value is inferred. The `x` value can also be
         *    omitted, in which case the inner arrays should be of length 2. Then the
         *    `x` value is automatically calculated, either starting at 0 and
         *    incremented by 1, or from `pointStart` and `pointInterval` given in the
         *    series options.
         *    ```js
         *       data: [
         *           [0, 1, 2],
         *           [1, 5, 5],
         *           [2, 0, 2]
         *       ]
         *    ```
         *
         * 2. An array of objects with named values. The following snippet shows only a
         *    few settings, see the complete options set below. If the total number of
         *    data points exceeds the series'
         *    [turboThreshold](#series.variwide.turboThreshold), this option is not
         *    available.
         *    ```js
         *       data: [{
         *           x: 1,
         *           y: 1,
         *           z: 1,
         *           name: "Point2",
         *           color: "#00FF00"
         *       }, {
         *           x: 1,
         *           y: 5,
         *           z: 4,
         *           name: "Point1",
         *           color: "#FF00FF"
         *       }]
         *    ```
         *
         * @sample {highcharts} highcharts/series/data-array-of-arrays/
         *         Arrays of numeric x and y
         * @sample {highcharts} highcharts/series/data-array-of-arrays-datetime/
         *         Arrays of datetime x and y
         * @sample {highcharts} highcharts/series/data-array-of-name-value/
         *         Arrays of point.name and y
         * @sample {highcharts} highcharts/series/data-array-of-objects/
         *         Config objects
         *
         * @type      {Array<Array<(number|string),number>|Array<(number|string),number,number>|*>}
         * @extends   series.line.data
         * @excluding marker
         * @product   highcharts
         * @apioption series.variwide.data
         */
        /**
         * The relative width for each column. On a category axis, the widths are
         * distributed so they sum up to the X axis length. On linear and datetime axes,
         * the columns will be laid out from the X value and Z units along the axis.
         *
         * @type      {number}
         * @product   highcharts
         * @apioption series.variwide.data.z
         */
        ''; // adds doclets above to transpiled file

        return VariwideSeries;
    });
    _registerModule(_modules, 'masters/modules/variwide.src.js', [_modules['Core/Globals.js'], _modules['Series/Variwide/VariwideSeries.js']], function (Highcharts, VariwideSeries) {

        var G = Highcharts;
        VariwideSeries.compose(G.Axis, G.Tick);

    });
}));